name: geyam_pos
description: GEYAM Smart POS — Flutter frontend (mobile + web)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  http_parser: ^4.0.2
  mime: ^2.0.0
  provider: ^6.1.2
  image_picker: ^1.1.2
  camera: ^0.11.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
